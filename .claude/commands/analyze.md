# Flutter Analyze

Run `flutter analyze` and report all errors and warnings. Group issues by severity (error → warning → info). For each error, suggest a fix if the cause is clear.

```bash
flutter analyze
```

After reporting, if there are errors, ask the user which ones to fix first.
